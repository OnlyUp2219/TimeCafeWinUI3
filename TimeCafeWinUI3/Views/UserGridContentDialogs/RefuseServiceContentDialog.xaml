<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TimeCafeWinUI3.Views.RefuseServiceContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:TimeCafeWinUI3.ViewModels"
    mc:Ignorable="d">
    <ScrollView>
        <StackPanel Spacing="12">
            <TextBox
                x:Name="ReasonTextBox"
                Height="100"
                AcceptsReturn="True"
                Header="Причина *"
                PlaceholderText="Введите причину отказа"
                Text="{x:Bind ViewModel.Reason, Mode=TwoWay}"
                TextWrapping="Wrap" />

            <TextBlock
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                Text="{x:Bind ViewModel.ErrorMessage}"
                TextWrapping="Wrap"
                Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
        </StackPanel>
    </ScrollView>
</Page> 