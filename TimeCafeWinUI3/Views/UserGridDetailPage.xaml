<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TimeCafeWinUI3.Views.UserGridDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="using:CommunityToolkit.WinUI.UI.Animations"
    xmlns:behaviors="using:TimeCafeWinUI3.Behaviors"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:customControls="using:TimeCafeWinUI3.Styles.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:TimeCafeWinUI3.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:TimeCafeWinUI3.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:TimeCafeWinUI3.Models"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:vm="using:TimeCafeWinUI3.ViewModels"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.BackgroundTransition>
            <BrushTransition />
        </Grid.BackgroundTransition>

        <ScrollViewer IsTabStop="True">
            <StackPanel x:Name="contentPanel" Margin="0,0,0,16">
                <RelativePanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                    <Grid
                        x:Name="itemHero"
                        Width="200"
                        Height="200"
                        Margin="{StaticResource SmallRightMargin}"
                        Padding="{StaticResource XSmallLeftTopRightBottomMargin}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        RelativePanel.AlignLeftWithPanel="True"
                        RelativePanel.AlignTopWithPanel="True">
                        <FontIcon
                            x:Name="detailedImage"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="96"
                            Glyph="&#xE716;"
                            Style="{Binding Item.StatusId, Converter={StaticResource StatusColorConverter}}" />
                    </Grid>

                    <TextBlock
                        x:Name="title"
                        Margin="{StaticResource XXSmallTopMargin}"
                        RelativePanel.AlignTopWithPanel="True"
                        Style="{ThemeResource TitleTextBlockStyle}"
                        Text="{x:Bind ViewModel.Item, Converter={StaticResource FullNameConverters}}" />

                    <StackPanel x:Name="propertiesGroup1">
                        <StackPanel x:Name="statusGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Статус" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.Status.StatusName}" />
                        </StackPanel>

                        <StackPanel x:Name="birthDateGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Дата рождения" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.BirthDate, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel x:Name="scudCardGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Номер карты SCUD" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.AccessCardNumber, Mode=OneWay}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel x:Name="propertiesGroup2">
                        <StackPanel x:Name="phoneGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Телефон" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.PhoneNumber, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel x:Name="emailGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Email" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.Email, Mode=OneWay}" />
                        </StackPanel>

                        <StackPanel x:Name="genderGroup" Margin="{StaticResource SmallTopMargin}">
                            <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="Пол" />
                            <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{x:Bind ViewModel.Item.Gender.GenderName}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel x:Name="propertiesGroup3">
                        <TextBlock
                            Margin="{StaticResource SmallTopMargin}"
                            Style="{ThemeResource SubtitleTextBlockStyle}"
                            Text="Дополнительная информация" />
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Item.ClientAdditionalInfos}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="{StaticResource SmallTopMargin}">
                                        <TextBlock
                                            Margin="{StaticResource SmallTopMargin}"
                                            Style="{ThemeResource SubtitleTextBlockStyle}"
                                            Text="Заметка" />
                                        <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Text="{Binding InfoText}" />
                                        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="{Binding CreatedAt, Converter={StaticResource DateTimeFormatConverter}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock
                            Margin="{StaticResource SmallTopMargin}"
                            Style="{ThemeResource BodyTextBlockStyle}"
                            Text="Нет информации"
                            Visibility="{x:Bind ViewModel.HasAdditionalInfo, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </StackPanel>
                </RelativePanel>

                <StackPanel
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    Spacing="8">
                    <StackPanel.Resources>
                        <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="Button">
                            <Setter Property="Transitions">
                                <Setter.Value>
                                    <TransitionCollection>
                                        <AddDeleteThemeTransition />
                                    </TransitionCollection>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}" />
                        </Style>
                    </StackPanel.Resources>

                    <Button Background="{ThemeResource SystemFillColorCriticalBrush}" Content="Удалить" />
                    <Button Click="EditButton_Click" Content="Изменить" />
                    <Button Click="RefuseButton_Click" Content="Отказ от услуг" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                        <Setter Target="title.(RelativePanel.RightOf)" Value="itemHero" />
                        <Setter Target="propertiesGroup1.(RelativePanel.RightOf)" Value="itemHero" />
                        <Setter Target="propertiesGroup1.(RelativePanel.Below)" Value="title" />
                        <Setter Target="propertiesGroup2.(RelativePanel.RightOf)" Value="propertiesGroup1" />
                        <Setter Target="propertiesGroup2.(RelativePanel.Below)" Value="title" />

                        <Setter Target="propertiesGroup3.(RelativePanel.Below)" Value="propertiesGroup2" />
                        <Setter Target="propertiesGroup1.Width" Value="300" />
                        <Setter Target="propertiesGroup2.Width" Value="300" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="title.(RelativePanel.Below)" Value="itemHero" />
                        <Setter Target="title.(RelativePanel.AlignBottomWithPanel)" Value="False" />
                        <Setter Target="title.(RelativePanel.AlignTopWithPanel)" Value="False" />

                        <Setter Target="propertiesGroup1.(RelativePanel.Below)" Value="title" />
                        <Setter Target="propertiesGroup2.(RelativePanel.Below)" Value="propertiesGroup1" />
                        <Setter Target="propertiesGroup3.(RelativePanel.Below)" Value="propertiesGroup2" />

                        <Setter Target="propertiesGroup1.Width" Value="300" />
                        <Setter Target="propertiesGroup2.Width" Value="300" />

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
