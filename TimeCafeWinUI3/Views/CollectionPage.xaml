<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TimeCafeWinUI3.Views.CollectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeCafeWinUI3.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Collection" Style="{ThemeResource TitleLargeTextBlockStyle}" Margin="20,20,20,0"/>

        <ListView x:Name="collection"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  Loaded="collection_Loaded"
                  ItemClick="collection_ItemClick"
                  Grid.Row="1"
                  Margin="20">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:CustomDataObject">
                    <Grid Margin="0,12,0,12" AutomationProperties.Name="{x:Bind Title}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="150"/>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <FontIcon x:Name="connectedElement" 
                                 FontFamily="{StaticResource SymbolThemeFontFamily}" 
                                 Glyph="&#xE8A5;" 
                                 FontSize="48"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>

                        <StackPanel Margin="12,0,0,0" Grid.Column="1">
                            <TextBlock Text="{x:Bind Title}" 
                                     Style="{ThemeResource SubtitleTextBlockStyle}" 
                                     HorizontalAlignment="Left" 
                                     Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Views: " 
                                         Style="{ThemeResource CaptionTextBlockStyle}" 
                                         FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind Views}" 
                                         Style="{ThemeResource CaptionTextBlockStyle}" 
                                         Margin="5,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Likes: " 
                                         Style="{ThemeResource CaptionTextBlockStyle}" 
                                         FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind Likes}" 
                                         Style="{ThemeResource CaptionTextBlockStyle}" 
                                         Margin="5,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind Description}" 
                                         Style="{ThemeResource BodyTextBlockStyle}" 
                                         FontStyle="Italic" 
                                         Margin="0,12,0,0" 
                                         TextTrimming="CharacterEllipsis" 
                                         MaxWidth="500" 
                                         MaxLines="1"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page> 